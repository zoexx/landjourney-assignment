{
  "name": "@lj/api",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "tsc -p tsconfig.json --noEmit",
    "dev": "vercel dev --listen 3001",
    "test": "echo \"(no api unit tests — domain tests live in packages/contracts)\""
  },
  "dependencies": {
    "@lj/contracts": "workspace:*",
    "@supabase/supabase-js": "^2.112.3",
    "zod": "^4.1.13"
  },
  "devDependencies": {
    "@types/node": "^22.15.3",
    "typescript": "~6.0.2"
  }
}
